name: fretbox_resident
description: TIET Resident App - Fretbox

publish_to: 'none'

version: 3.10.10+210

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  percent_indicator: ^4.2.3
  mobile_scanner: ^6.0.2
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
